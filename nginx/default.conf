server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Health check endpoint (used by Kubernetes)
    location /health {
        return 200 'ok';
        add_header Content-Type text/plain;
    }
}